import React from 'react'
// import Cell from './cell'

function Cal({props}) {
    return (
        <div className="cards_container">
            Calendar
          {/* {props.map( prop => (
            // <Cell
            // key={prop.id}
            // class={prop.class}
            // />
          ))} */}
        </div>
  )
}

export default Cal
